import { CommonCategoryModel } from './common-category-model';

describe('CommonCategoryModel', () => {
  it('should create an instance', () => {
    expect(new CommonCategoryModel()).toBeTruthy();
  });
});
