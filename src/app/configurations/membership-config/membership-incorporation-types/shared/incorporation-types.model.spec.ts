import { IncorporationTypes } from './incorporation-types.model';

describe('IncorporationTypes', () => {
  it('should create an instance', () => {
    expect(new IncorporationTypes()).toBeTruthy();
  });
});
