import { LandOwnersipType } from './land-ownersip-type.model';

describe('LandOwnersipType', () => {
  it('should create an instance', () => {
    expect(new LandOwnersipType()).toBeTruthy();
  });
});
