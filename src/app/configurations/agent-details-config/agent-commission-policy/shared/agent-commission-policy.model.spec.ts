import { AgentCommissionPolicy } from './agent-commission-policy.model';

describe('AgentCommissionPolicy', () => {
  it('should create an instance', () => {
    expect(new AgentCommissionPolicy()).toBeTruthy();
  });
});
