import { AgentSecurityConfig } from './agent-security-config.model';

describe('AgentSecurityConfig', () => {
  it('should create an instance', () => {
    expect(new AgentSecurityConfig()).toBeTruthy();
  });
});
