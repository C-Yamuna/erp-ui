import { RepaymentFrequency } from './repayment-frequency.model';

describe('RepaymentFrequency', () => {
  it('should create an instance', () => {
    expect(new RepaymentFrequency()).toBeTruthy();
  });
});
