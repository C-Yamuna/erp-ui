import { ScaleOfFinanceConfig } from './scale-of-finance-config.model';

describe('ScaleOfFinanceConfig', () => {
  it('should create an instance', () => {
    expect(new ScaleOfFinanceConfig()).toBeTruthy();
  });
});
