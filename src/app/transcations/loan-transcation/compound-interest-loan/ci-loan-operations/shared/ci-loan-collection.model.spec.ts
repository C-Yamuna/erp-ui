import { CiLoanCollection } from './ci-loan-collection.model';

describe('CiLoanCollection', () => {
  it('should create an instance', () => {
    expect(new CiLoanCollection()).toBeTruthy();
  });
});
