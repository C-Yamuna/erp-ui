import { SaoLoanApplication } from './sao-loan-application.model';

describe('SaoLoanApplication', () => {
  it('should create an instance', () => {
    expect(new SaoLoanApplication()).toBeTruthy();
  });
});
