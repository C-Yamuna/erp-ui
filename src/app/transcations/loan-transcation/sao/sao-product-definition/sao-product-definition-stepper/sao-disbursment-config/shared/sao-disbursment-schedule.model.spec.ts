import { SaoDisbursmentSchedule } from './sao-disbursment-schedule.model';

describe('SaoDisbursmentSchedule', () => {
  it('should create an instance', () => {
    expect(new SaoDisbursmentSchedule()).toBeTruthy();
  });
});
