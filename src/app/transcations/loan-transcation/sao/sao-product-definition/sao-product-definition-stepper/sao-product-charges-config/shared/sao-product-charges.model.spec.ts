import { SaoProductCharges } from './sao-product-charges.model';

describe('SaoProductCharges', () => {
  it('should create an instance', () => {
    expect(new SaoProductCharges()).toBeTruthy();
  });
});
