import { SaoInterestPolicyConfig } from './sao-interest-policy-config.model';

describe('SaoInterestPolicyConfig', () => {
  it('should create an instance', () => {
    expect(new SaoInterestPolicyConfig()).toBeTruthy();
  });
});
