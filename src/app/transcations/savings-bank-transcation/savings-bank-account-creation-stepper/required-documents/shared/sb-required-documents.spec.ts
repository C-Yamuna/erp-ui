import { SbRequiredDocuments } from './sb-required-documents';

describe('SbRequiredDocuments', () => {
  it('should create an instance', () => {
    expect(new SbRequiredDocuments()).toBeTruthy();
  });
});
