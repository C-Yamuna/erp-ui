import { SavingsBankNomineeModel } from './savings-bank-nominee-model';

describe('SavingsBankNomineeModel', () => {
  it('should create an instance', () => {
    expect(new SavingsBankNomineeModel()).toBeTruthy();
  });
});
