import { SavingsBankServiceModel } from './savings-bank-service-model';

describe('SavingsBankServiceModel', () => {
  it('should create an instance', () => {
    expect(new SavingsBankServiceModel()).toBeTruthy();
  });
});
