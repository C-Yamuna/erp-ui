import { SavingBankApplicationModel } from './saving-bank-application-model';

describe('SavingBankApplicationModel', () => {
  it('should create an instance', () => {
    expect(new SavingBankApplicationModel()).toBeTruthy();
  });
});
