import { SavingsBankKycModel } from './savings-bank-kyc-model';

describe('SavingsBankKycModel', () => {
  it('should create an instance', () => {
    expect(new SavingsBankKycModel()).toBeTruthy();
  });
});
