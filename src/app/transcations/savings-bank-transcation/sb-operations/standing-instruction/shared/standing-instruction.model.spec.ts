import { StandingInstruction } from './standing-instruction.model';

describe('StandingInstruction', () => {
  it('should create an instance', () => {
    expect(new StandingInstruction()).toBeTruthy();
  });
});
