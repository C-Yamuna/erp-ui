import { FdCumulativeJointHolder } from './fd-cumulative-joint-holder.model';

describe('FdCumulativeJointHolder', () => {
  it('should create an instance', () => {
    expect(new FdCumulativeJointHolder()).toBeTruthy();
  });
});
