import { RequiredDocuments } from './required-documents.model';

describe('RequiredDocuments', () => {
  it('should create an instance', () => {
    expect(new RequiredDocuments()).toBeTruthy();
  });
});
