import { RdAccountTransaction } from './rd-account-transaction.model';

describe('RdAccountTransaction', () => {
  it('should create an instance', () => {
    expect(new RdAccountTransaction()).toBeTruthy();
  });
});
