import { SharesInvestments } from './shares-investments.model';

describe('SharesInvestments', () => {
  it('should create an instance', () => {
    expect(new SharesInvestments()).toBeTruthy();
  });
});
