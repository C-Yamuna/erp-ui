import { AccountRequriedDocuments } from './account-requried-documents.model';

describe('AccountRequriedDocuments', () => {
  it('should create an instance', () => {
    expect(new AccountRequriedDocuments()).toBeTruthy();
  });
});
