import { AccountsChequeDetails } from './accounts-cheque-details.model';

describe('AccountsChequeDetails', () => {
  it('should create an instance', () => {
    expect(new AccountsChequeDetails()).toBeTruthy();
  });
});
