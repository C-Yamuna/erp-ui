import { JointAccountHolder } from './joint-account-holder.model';

describe('JointAccountHolder', () => {
  it('should create an instance', () => {
    expect(new JointAccountHolder()).toBeTruthy();
  });
});
