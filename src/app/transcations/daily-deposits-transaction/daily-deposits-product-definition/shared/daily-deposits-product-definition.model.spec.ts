import { DailyDepositsProductDefinition } from './daily-deposits-product-definition.model';

describe('DailyDepositsProductDefinition', () => {
  it('should create an instance', () => {
    expect(new DailyDepositsProductDefinition()).toBeTruthy();
  });
});
